{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "packageRules": [
    {
      "description": "Keep the checkout default on the Python version recommended for MLX",
      "matchManagers": ["pyenv"],
      "enabled": false
    }
  ]
}
