# 更新记录

[English](CHANGELOG.md) | 简体中文

只有创建带标签的发行版本后才添加版本标题和日期。当前应用版本为 `0.1.0`，不代表已经发布 Git 标签或 GitHub Release。

## 未发布

- 将尚未发布的项目、Python 包、模块、CLI 与随包 Skill 从 Rara 统一更名为 raradio。
- 统一项目元数据、GitHub 链接与源码仓库安装说明。
- 增加只读的 `raradio setup` 上手检查，并提供以无需 Ollama 的单人朗读为主线的独立中英文安装指南。

### 小说导入与复核

- 支持日文章标题、可恢复的引号异常诊断及常见组合字符的安全切分，保持原文与偏移完整。
- 支持按语义纠正旁白/对白分类，包括无引号对白，并提供人工调整章节和混合说话人片段边界的结构入口。
- 支持显式旧文本编码、UTF-32 BOM、关闭标题识别，并在续跑时复用已保存的导入选项。
- 显式重新分析保留人工归属，失败或中断时阻止旧音频绕过检查；仅调整章节时保留已有音频与配音判断。
- 新项目未指定声音语言时采用 `auto`，旧项目保留中文默认值；置信度阈值实际生效，非法角色别名在保存前被拒绝。
- 新增无标记小说、日文输入和翻译边界的双语说明。

### 初始 Alpha 工作流

- 用 headless 命令完成 TXT 导入、说话人与配音表审核、分段合成、断点续跑和章节 WAV 导出。
- 仅依赖 Python 标准库的核心，以及无需模型或可选服务即可跑通流程的诊断音后端。
- Apple Silicon 上可选的 MLX Qwen3-TTS 合成，支持预设声音与参考音频配置；可选 Ollama 分析。
- 中英文指南与原创样例，覆盖旁白、对白审核、局部重生成和缓存复用。

### 开源准备

- 覆盖已有配音配置前校验声音字段类型；复用校验通过的参考录音，并以原子方式保存新副本，导入中断时保留已有工作。
- 将 Ollama HTTP 协议异常记录为待审核问题，并保持诊断错误的结构化输出；同步手册中的显式编码、结构修复和重分析选项。
- 随包提供 Agent Skill 和 `raradio agent-guide`（Markdown/JSON），说明如何利用用户已有 Agent 经 CLI 制作与修订，无需 Ollama。
- 修正缺失或过期音频在限量续跑时的剩余工作报告；统一启动器按调用者目录解释路径，并简洁报告无效配置。
- 明确 CLI 优先的定位、平台要求、安装路径，以及同类项目的取舍。
- 统一包版本来源，提交依赖锁文件，增加安装前置检查，区分核心与可选服务的环境诊断。
- 配置 macOS/Linux 源码仓库锁定安装、无模型测试，以及可选的 MLX 依赖检查。
- 依赖更新提案，以及发布和项目格式兼容策略。

项目仍处于 Alpha。自动化流程检查和短语音样例不能证明整本书的声音质量或广泛的硬件兼容性。实际验证范围见[测试与验证](docs/verification.zh-CN.md)。
